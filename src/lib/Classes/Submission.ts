import type HasSubmissionData from "../Interfaces/HasSubmissionData";

/**
 * Submission class.
 * 
 * @since 1.0.0
 */
export default class Submission {
    /**
     * Submission data.
     * 
     * @param submissionData Submission data.
     * 
     * @since 1.0.0
     */
    public constructor( submissionData: HasSubmissionData ) {}

    /**
     * Submit data.
     * 
     * @param data 
     * 
     * @since 1.0.0
     */
    public submit( data: [] ) {

    }
}